3. styles.css
(Explain: Basic styling for visual appeal)

CopyRun
body {
    font-family: Arial, sans-serif;
    line-height: 1.6;
    margin: 0;
    padding: 0;
    background-color: #e0f7fa; /* Light blue background representing sky */
}

header {
    background-color: #00695c; /* Dark teal for header background */
    color: white;
    padding: 20px;
    text-align: center;
}

.header-img {
    width: 100%;
    max-height: 400px;
    object-fit: cover;
    margin-top: 10px;
}

section {
    padding: 20px;
    margin: 10px;
    background-color: #ffffff; /* White background for sections */
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

.section-img {
    width: 100%;
    height: auto;
    border-radius: 8px;
    margin-top: 10px;
}

footer {
    text-align: center;
    padding: 10px;
    background-color: #004d40; /* Darker shade for footer */
    color: white;
}
(Add explain: Image showing styled webpage with sections)
